# wdio-web-test
WebdriverIO | Cucumber | Allure-Reports - Web Testing Framework

# Commands
  - `npm run test` - It will execute all test cases from `src/features` directory
  - `npm run script` - It will execute test cases `'@login` cucumber tags
  - `npm run report` - It will generate reports & open on local server